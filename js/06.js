//Objeto Math

let resultado;


resultado = Math.PI;
resultado = Math.round(2.3);
resultado = Math.ceil(2.9); //Redondea hacia arriba
resultado = Math.floor(2.9); //Redondea hacia abajo
resultado = Math.sqrt(144); //Da la razin cuadrada
resultado = Math.abs (-200); //Convierte negativo en positivo
resultado = Math.min (3, 4 ,5, 9, 7, 29); //Obtiene el valor mas bajo
resultado = Math.max (3, 4 ,5, 9, 7, 29); //Obtiene el valor mas alto
resultado = Math.floor (Math.random () * 30) //Emplea varias operaciones


console.log(resultado);